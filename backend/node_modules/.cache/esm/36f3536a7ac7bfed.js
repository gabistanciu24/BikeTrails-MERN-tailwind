let uploadPicture,Comment,Post,fileRemover,uuidv4;_7ac‍.x([["createPost",()=>createPost],["updatePost",()=>updatePost],["deletePost",()=>deletePost]]);_7ac‍.w("../middleware/uploadPictureMiddleware",[["uploadPicture",["uploadPicture"],function(v){uploadPicture=v}]]);_7ac‍.w("../models/Comment",[["default",["Comment"],function(v){Comment=v}]]);_7ac‍.w("../models/Post",[["default",["Post"],function(v){Post=v}]]);_7ac‍.w("../utils/fileRemover",[["fileRemover",["fileRemover"],function(v){fileRemover=v}]]);_7ac‍.w("uuid",[["v4",["uuidv4"],function(v){uuidv4=v}]]);





       const createPost = async (req, res, next) => {
  try {
    const post = new Post({
      title: "sample title",
      caption: "sample caption",
      slug: uuidv4(),
      body: {
        type: "doc",
        content: [],
      },
      photo: "",
      user: req.user._id,
    });
    const createdPost = await post.save();
    return res.json(createdPost);
  } catch (error) {
    // return res.status(500).json({ message: "Something went wrong!" });
    next(error);
  }
};

       const updatePost = async (req, res, next) => {
  try {
    const post = await Post.findOne({ slug: req.params.slug });

    if (!post) {
      const error = new Error("Post not found");
      next(error);
      return;
    }

    const upload = uploadPicture.single("postPicture");

    const handleUpdatePostData = async (data) => {
      const { title, caption, slug, body, tags, categories } = JSON.parse(data);
      post.title = title || post.title;
      post.caption = caption || post.caption;
      post.slug = slug || post.slug;
      post.body = body || post.body;
      post.tags = tags || post.tags;
      post.categories = categories || post.categories;
      const updatedPost = await post.save();
      return res.json(updatedPost);
    };

    upload(req, res, async function (err) {
      if (err) {
        const error = new Error(
          "An unknown error occured when uploading " + err.message
        );
        next(error);
      } else {
        // everything went well
        if (req.file) {
          let filename;
          filename = post.photo;
          if (filename) {
            fileRemover(filename);
          }
          post.photo = req.file.filename;
          handleUpdatePostData(req.body.document);
        } else {
          let filename;
          filename = post.photo;
          post.photo = "";
          fileRemover(filename);
          handleUpdatePostData(req.body.document);
        }
      }
    });
  } catch (error) {
    // return res.status(500).json({ message: "Something went wrong!" });
    next(error);
  }
};

       const deletePost = async (req, res, next) => {
  try {
    const post = await Post.findOneAndDelete({
      slug: req.params.slug,
    });

    if (!post) {
      const error = new Error("Post not found");
      return next(error);
    }

    await Comment.deleteMany({ post: post._id });
    return res.json({ message: "Post is successfully deleted" });
  } catch (error) {
    next(error);
  }
};
